@model MyProject.Models.Category

<h2>Category Details</h2>

<dl class="row">
    <dt class="col-sm-2">
        @Model.Name
    </dt>
    <dd class="col-sm-10">
        @Model.Name
    </dd>
</dl>

<div>
    <a asp-action="Edit" asp-route-id="@Model.CategoriesId" class="btn btn-primary">Edit</a>
    <a asp-action="Index" class="btn btn-secondary">Back to List</a>
</div>